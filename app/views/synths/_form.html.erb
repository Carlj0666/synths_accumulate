  


<% if @synth.errors.any? %>
<div id="error_explanation">
  <h2><%= pluralize(@synth.errors.count, "error") %> prohibited this Synth from being saved:</h2>

  <ul>
    <% @synth.errors.full_messages.each do |error| %>
      <li><%= error %></li>
    <% end %>
  </ul>
</div>
<% end %>

<%= form_for(@synth) do |f| %>
<%= f.label :name %>
<%= f.text_field :name %><br>
<%= f.label :tech %>
<%= f.collection_select :tech_id, Tech.all, :id, :name, prompt: true %>
New<%= f.fields_for :tech do |t| %>
  <%= t.label :tech %>
  <%= t.text_field :name %><br>
<% end %>
<br>
<%= f.label :brand %>
<%= f.text_field :brand %><br> 
<%= f.label :hybrid %>
<%= f.check_box :hybrid %><br>
<%= f.label :price %>
<%= f.number_field :price %><br>
<%= f.label :description %>
<%= f.text_field :description %><br>
<%= f.submit %>
<% end %>